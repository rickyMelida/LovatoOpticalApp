<div class="row g-2">
    <div class="col-md-8">
        <label class="form-label">Documento</label>
        <input type="text" class="form-control" id="docInput" placeholder="Ej. 12345678">
    </div>

    <div class="col-md-4 d-grid align-items-end">
        <button type="button" class="btn btn-outline-primary mt-md-4" id="btn-search" onclick="searchPatient()">
            <i class="bi bi-search me-1"></i>
            Buscar
        </button>
    </div>
</div>

<div class="alert alert-success mt-3 d-none" id="pacienteEncontrado">
    <div class="d-flex justify-content-between align-items-start gap-3">
        <div>
            <strong id="pacNombre"></strong><br>
            <span class="small">Documento: <span id="pacDoc"></span></span>
        </div>
        <span class="badge text-bg-success">Cliente existente</span>
    </div>
</div>

<div class="alert alert-warning mt-3 d-none" id="pacienteNoEncontrado">
    <div class="d-flex justify-content-between align-items-center gap-3 flex-wrap">
        <div>
            <strong>No se encontró ningún cliente con ese documento.</strong>
            <div class="small">¿Deseas registrarlo ahora?</div>
        </div>
        <button type="button" class="btn btn-sm btn-outline-primary" id="btnShowNewPatientForm" onclick="showNewPatientForm()">
            <i class="bi bi-person-plus me-1"></i>
            Sí, agregar cliente
        </button>
    </div>
</div>

<div id="formNuevoPaciente" class="mt-3 d-none">
    <hr>

    <h3 class="h6 mb-3">Registrar nuevo cliente</h3>

    <div class="row g-3">
        <form id="newCustomerForm">
            @await Html.PartialAsync("Forms/_NewCustomer")
        </form>
    </div>

    <button type="button" class="btn btn-primary mt-3 float-start" id="btnCreateCustomer" onclick="createPatient()">
        <i class="bi bi-person-plus me-1"></i>
        Crear cliente
    </button>
</div>
